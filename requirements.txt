fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Local Qwen3-Omni inference
transformers==4.57.3
accelerate>=0.26
qwen-omni-utils

# Optional but recommended for speed (GPU only):
# flash-attn
