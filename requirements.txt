fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Used for feature extraction + PCM conversion.
numpy>=1.24

# NOTE:
# - We do NOT install/launch Qwen3-Omni here.
# - Install the team's inference package separately (it will use GPU).
# - Make sure your environment already has an appropriate PyTorch build.
